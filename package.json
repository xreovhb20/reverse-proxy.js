{
  "description": "Simple reverse proxy server supporting WebSockets.",
  "homepage": "https://github.com/cedx/reverse-proxy.js",
  "name": "@cedx/reverse-proxy",

  "author": "Cédric Belin <cedric@belin.io>",
  "bugs": "https://github.com/cedx/reverse-proxy.js/issues",
  "license": "Apache-2.0",
  "preferGlobal": true,
  "repository": "cedx/reverse-proxy.js",
  "version": "1.2.0",

  "jsnext:main": "./src/index",
  "main": "./lib/index",

  "bin": {
    "reverse-proxy": "./bin/cli.js"
  },
  "dependencies": {
    "commander": "^2.9.0",
    "forever": "^0.15.3",
    "http-proxy": "^1.16.2",
    "js-yaml": "^3.8.1",
    "rxjs": "^5.1.1"
  },
  "devDependencies": {
    "@cedx/coveralls": "^0.2.0",
    "@cedx/gulp-david": "^4.0.0",
    "babel-preset-latest": "^6.22.0",
    "babel-register": "^6.23.0",
    "del": "^2.2.2",
    "esdoc": "^0.5.2",
    "gulp": "^3.9.1",
    "gulp-babel": "^6.1.2",
    "gulp-istanbul": "^1.1.1",
    "gulp-eslint": "^3.0.1",
    "gulp-load-plugins": "^1.5.0",
    "gulp-mocha": "^3.0.1",
    "isparta": "^4.0.0"
  },
  "engines": {
    "node": ">=6.9.0"
  },
  "keywords": [
    "http",
    "proxy",
    "reverse",
    "routing",
    "server",
    "websocket"
  ],
  "scripts": {
    "coverage": "coveralls --file=var/lcov.info",
    "prepublish": "gulp build",
    "restart": "npm stop && gulp clean && npm start",
    "start": "forever start -e var/stderr.log -o var/stdout.log -l reverse-proxy.log --append --minUptime 30000 --spinSleepTime 30000 --uid reverse-proxy bin/cli.js",
    "stop": "forever stop reverse-proxy",
    "test": "gulp test"
  }
}
